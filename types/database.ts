/**
 * Placeholder types until we generate real ones from your live schema.
 *
 * Once the migration in supabase/migrations/0001_init.sql has been run
 * against your project, generate accurate types with:
 *
 *   npx supabase gen types typescript --project-id <your-project-ref> > types/database.ts
 *
 * (Project ref is the string in your Supabase project URL, e.g.
 * https://<project-ref>.supabase.co)
 *
 * Until then, this keeps the app compiling without lying about shape.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
